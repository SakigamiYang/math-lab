# Data policy

Raw data files are not committed to git.

- If the experiment needs external data, provide a script or instructions to fetch/generate it.
- Record data provenance and hashes in `artifacts/metadata.json` when relevant.
